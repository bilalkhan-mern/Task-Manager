1) create the project in vite
to run frontend code :- (npm run dev)

2) for backend code :- (npm run dev)  //use nodemon(package.json)

explanation :- 

1) sir first of all created the database architecture with all the schema and odels with the help of mongoose
2) after that created the server and routes 
3) on the basis of routes the controllers will work with business logic in it with database integration
3) and when created all the routes i had created front end and while solving the errors again and again i finally get the output not 100% but i tried my best 
4) and one thing i got difficulties in update where their is a condition os status which i tried but i was lost anywhere in between 
5) and i was totally doing the task in that way that if the code execeuted successfully but if what if it doesn't so i tried to handle both the sides.

